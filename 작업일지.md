# 횡성우림측량 홈페이지 작업일지

> 다음 작업 시작 전 이 파일을 먼저 읽어주세요.

---

## 사이트 기본 정보

| 항목 | 내용 |
|------|------|
| 공개 사이트 (정식 도메인) | https://www.urimsurvey.co.kr |
| 공개 사이트 (Vercel 기본 주소) | https://urim-website.vercel.app |
| 로컬 실행 주소 | http://localhost:3000 |
| GitHub | https://github.com/urim-survey/urim-website |
| 로컬 폴더 | C:\Users\c\Desktop\urim-survey |
| 관리자 이메일 | ppka4242@gmail.com |
| 도메인 구매처 | 가비아(Gabia), DNS도 가비아에서 관리 |

### 로컬 서버 실행 방법
1. VS Code 열기
2. 터미널에서 `npm run dev` 입력
3. 브라우저에서 http://localhost:3000 접속

### Vercel 배포 방법
- 클로드에게 **"푸시해줘"** 라고 하면 자동으로 GitHub → Vercel 배포

---

## 작업일지

---

### 2026-07-17

#### 블로그 시스템 전체 재구축 (Supabase → Git 파일 기반)
- 글 저장 방식을 Supabase DB → 저장소 안 마크다운 파일(`content/posts/*.md`)로 전환
- 대표·본문 이미지도 Supabase Storage → `public/blog-images/` 폴더로 전환 (저장소에 같이 커밋됨)
- `/blog`, `/blog/[slug]`를 완전 정적 페이지로 전환 (revalidate 제거, `generateStaticParams` 사용) → 방문자가 봐도 Vercel 함수 호출 0번
- 글쓰기(`/blog/write`)·수정(`/blog/[slug]/edit`)은 **로컬(`npm run dev`)에서만 동작**, 배포 사이트에서는 완전 차단(404) — 글 작성 후 `git push`해야 사이트에 반영됨
- `src/lib/posts.ts` (파일 기반 CRUD), `/api/posts`, `/api/posts/[slug]`, `/api/posts/upload-image` 라우트 신규 추가 (전부 로컬 전용)

#### Vercel 사용량 절감
- `next.config.ts`에서 이미지 최적화(Image Optimization) 끔 (`unoptimized: true`) — 무료 요금제에서 가장 빨리 소진되는 항목이라 미리 차단

#### 문의상담 → 전화상담으로 전면 교체
- 기존 문의폼(이름·연락처·문의유형·내용 입력) 완전히 제거
- "전화 상담하기"(검정) + "카카오톡 상담하기"(카카오 브랜드 컬러 `#FEE500`) 버튼으로 대체
- 카카오톡 채널은 아직 미개설 — 채널 만드신 후 URL을 `src/app/contact/page.tsx`의 `KAKAO_CHANNEL_URL` 상수에 넣기만 하면 연결됨
- 구글 지도 iframe 임베드 추가 (API 키 필요 없는 방식, 비용 없음)

#### Supabase 완전 제거
- `/api/contact`, `src/lib/supabase.ts`, `src/lib/supabase-server.ts` 삭제
- `@supabase/supabase-js` 패키지 제거 (프로젝트에서 Supabase 의존성 없음)
- 기존 Supabase `posts`/`contacts` 테이블과 `blog-images` 버킷 데이터는 남아있지만 더 이상 코드에서 사용 안 함 — 필요 없으면 Supabase 콘솔에서 직접 정리 가능

#### 보안 점검 및 수정
- 글 저장·조회·삭제 API의 슬러그 값 경로 탈출(path traversal) 취약점 발견·수정 (`src/lib/posts.ts`에 슬러그 검증 추가)
- 나머지(이미지 확장자 미검증, 비밀번호 시도 제한 없음)는 로컬 전용 기능 + 위험도 낮아 보류

#### 커스텀 도메인 연결
- 가비아에서 `urimsurvey.co.kr` 구매 후 Vercel에 연결 완료
- 가비아 DNS에 A 레코드(`@` → `76.76.21.21`), CNAME(`www` → `cname.vercel-dns.com.`) 등록
- `www.urimsurvey.co.kr`가 정식 주소(Production), 루트 도메인은 자동으로 www로 리다이렉트(308)

#### SEO 개선
- 페이지마다 고유 제목·설명 추가 (그동안 전 페이지 제목이 "횡성우림측량"으로 동일했음)
- 블로그 글마다 자동으로 고유 제목·설명·대표이미지 메타데이터 생성 (`generateMetadata`)
- `sitemap.xml`, `robots.txt` 신규 생성 (새 블로그 글 자동 반영)
- 지역 업체 구조화 데이터(JSON-LD, ProfessionalService) 추가 — 상호명·전화·주소·운영시간
- Open Graph 태그 추가 — 카카오톡/문자 공유 시 제목·설명·사진 미리보기 표시됨
- `src/lib/site.ts`에 사이트 URL·이름 등 공통 상수 정리

---

### 2026-07-04

#### 블로그 시스템 전체 구축
- `/blog/write` 글쓰기 페이지 신규 생성 → 발행 시 Supabase `posts` 테이블 저장
- TipTap 리치텍스트 에디터: 한글 폰트 24종·폰트 크기(8~72px)·굵게·기울이기·밑줄·취소선·정렬·색상·목록·링크·인용·코드
- 본문 이미지: 컴퓨터에서 업로드 → Supabase Storage `blog-images` 버킷 저장
- 본문 이미지 **크기 조절** (드래그 핸들) + **정렬** (왼/가운데/오른) 기능
- 대표 이미지 **변경·제거** 버튼 추가

#### 블로그 상세 페이지 개편 (`/blog/[slug]`)
- 제목 맨 위·가운데 정렬 → 썸네일(블러 효과) → 본문 순서로 재배치
- 하단 **다른 글 보기** 최신 3개 + **목록으로** 버튼 추가

#### 버그 수정
- 블로그 상세 404 오류: 한글 슬러그 URL 인코딩 문제 → `decodeURIComponent` 적용
- 새 글 슬러그 영문+타임스탬프 방식으로 변경 (한글 URL 재발 방지)
- 히어로 이미지 검정 깜빡임: CSS background → `<Image priority>` 교체

#### 배포 환경 정상화
- Vercel이 `main` 브랜치 배포 중인데 `master`에만 푸시하던 문제 발견·수정
- **앞으로 푸시 시 `master`와 `main` 동시 업데이트**

#### 작업 운영 개선
- `작업일지.md` 신규 생성 (매 세션 종료 시 자동 업데이트)
- "오늘 작업 끝" 명령 시 작업일지 업데이트 + master/main 동시 푸시 자동화

#### Vercel 빌드 오류 수정 (세션 3)
- `contact/route.ts` : `supabaseAdmin` import 경로 오류 → `supabase-server.ts`로 수정
- `ResizableImage.tsx` : TipTap TypeScript 타입 불일치 → `NodeViewProps`, `CommandProps` 적용
- 로컬 `npm run build` 통과 확인 후 배포 → Vercel 정상 배포 완료

---

### 이전 작업 (2026년 6월)

| 날짜 | 내용 |
|------|------|
| 6월 말 | 전체 사이트 디자인 리뉴얼 (모노톤, Pretendard) |
| 6월 말 | 회사소개·서비스·업무절차·FAQ·문의·블로그 페이지 완성 |
| 6월 말 | 모바일 하단 고정 네비게이션 + 전화 플로팅 버튼 추가 |
| 6월 말 | 실제 사이트 이미지 자산 적용 |

---

## 현재 파일 구조 (중요 파일)

```
urim-survey/
├── content/
│   └── posts/                    ← 블로그 글 마크다운 파일 (.md) ★ NEW
├── public/
│   └── blog-images/              ← 블로그 이미지 파일 ★ NEW
├── src/
│   ├── app/
│   │   ├── page.tsx              ← 홈페이지
│   │   ├── about/page.tsx        ← 회사소개
│   │   ├── services/page.tsx     ← 서비스안내
│   │   ├── guide/page.tsx        ← 업무절차
│   │   ├── blog/
│   │   │   ├── page.tsx          ← 블로그 목록 (정적)
│   │   │   ├── write/
│   │   │   │   ├── page.tsx      ← 로컬에서만 열림 (프로덕션 404)
│   │   │   │   └── WriteForm.tsx ← 실제 글쓰기 화면
│   │   │   └── [slug]/
│   │   │       ├── page.tsx      ← 블로그 상세 (정적)
│   │   │       ├── PostActions.tsx ← 수정·삭제 버튼
│   │   │       └── edit/
│   │   │           ├── page.tsx      ← 로컬에서만 열림 (프로덕션 404)
│   │   │           └── EditForm.tsx  ← 실제 수정 화면
│   │   ├── faq/page.tsx          ← FAQ
│   │   ├── contact/page.tsx      ← 문의상담 (전화·카카오톡·지도, 폼 없음) ★ 변경
│   │   └── api/
│   │       ├── auth/route.ts     ← 글쓰기 비밀번호 확인 (로컬 전용)
│   │       └── posts/            ← 글 저장·수정·삭제·이미지 업로드 (로컬 전용) ★ NEW
│   ├── components/
│   │   ├── TipTapEditor.tsx      ← 글쓰기 에디터
│   │   ├── ResizableImage.tsx    ← 이미지 크기조절
│   │   ├── BlogCard.tsx          ← 블로그 카드
│   │   ├── HeroSection.tsx       ← 히어로 섹션
│   │   └── Header.tsx / Footer.tsx
│   ├── lib/
│   │   ├── posts.ts              ← 블로그 글 파일 읽기·쓰기 함수 (경로 검증 포함)
│   │   └── site.ts               ← 사이트 URL·이름 등 공통 상수 ★ NEW
│   ├── sitemap.ts                ← SEO: sitemap.xml 자동 생성 ★ NEW
│   └── robots.ts                 ← SEO: robots.txt 자동 생성 ★ NEW
├── .env.local                    ← API 키 (절대 공유 금지)
├── 작업일지.md                   ← 이 파일
└── CLAUDE.md                     ← 클로드 작업 지침
```

> Supabase 클라이언트(`lib/supabase.ts`, `lib/supabase-server.ts`)는 2026-07-17에 삭제됨 — 더 이상 프로젝트에서 Supabase를 쓰지 않음.

---

## 다음에 할 수 있는 작업 목록

- [x] 블로그 글 **수정·삭제** 기능 (로컬 전용으로 완료)
- [x] 커스텀 도메인 연결 (`urimsurvey.co.kr`, DNS 등록까지 완료 — SSL 인증서 발급 상태만 Vercel에서 확인하면 끝)
- [x] SEO 기본 정비 (페이지별 메타데이터, sitemap, robots.txt, 구조화 데이터, Open Graph)
- [ ] **구글 서치 콘솔**(search.google.com/search-console)에 사이트 등록 + sitemap 제출 — 아직 안 함
- [ ] 블로그 **카테고리** 필터 (업무사례·업계소식·공지사항·홍보)
- [ ] 카카오톡 채널 개설 후 URL을 `src/app/contact/page.tsx`의 `KAKAO_CHANNEL_URL`에 연결
- [ ] 블로그 본문에 삽입하는 이미지들의 대체텍스트(alt) — 우선순위 낮음, SEO 점검 때 보류함
- [ ] 테스트로 작성한 블로그 글 "테스트1~7" 정리 여부 결정 (현재 실사이트에 그대로 노출 중, 사장님이 남겨두기로 하셨음)
- [ ] (선택) Supabase 콘솔에서 이제 안 쓰는 옛 데이터 정리
- ~~문의상담 폼 → Supabase `contacts` 테이블 연동~~ → 문의폼 자체를 없애고 전화상담으로 전환해서 해당 없음

---

## Supabase 정보 (2026-07-17부터 미사용)

프로젝트 코드에서는 Supabase를 더 이상 쓰지 않습니다 (블로그는 Git 파일, 문의는 전화상담으로 전환).
기존 데이터는 아래 프로젝트에 그대로 남아있으니, 완전히 정리하고 싶으면 Supabase 콘솔에서 직접 삭제하면 됩니다.

| 항목 | 내용 |
|------|------|
| 프로젝트 URL | https://twlgilmkqxzikvnzkisd.supabase.co |
| 남아있는 테이블 | `posts` (구 블로그 글), `contacts` (구 문의 내역) |
| 남아있는 Storage 버킷 | `blog-images` (구 블로그 이미지) |

---

*마지막 업데이트: 2026-07-17*
